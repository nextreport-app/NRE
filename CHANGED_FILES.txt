CHANGED_FILES.txt — commit 39589fb "Redesign Report Summary card and download screen; rename custom-title link"

src/components/report-upload-wizard.tsx
  This was new UI work per an explicit design spec, not a restore — no
  git-history source commit involved (unlike the prior two rounds).

  1. Fix 1 — Report Summary card (Step 5, before generating):
     - Campaigns row: campaign names now listed below the "N selected"
       count, one per line, 12px muted grey (#64748b), indented (pl-4).
     - New "Week period" row: shown when reportType === "WEEKLY" and a
       weekly range is available, using the wizard's own weeklyRangeIso
       state.
     - New "Month to date" row: shown whenever mtdRange is available.
     - Comparison reports show a "Comparison periods: A vs B" row instead
       of Week period/Month to date (comparisonData.periodALabel/
       periodBLabel).
     - New "Estimated slides" row via a new estimatedSlideCount()
       function: cover(1) + campaign count + selectedAdSets.size +
       chart(1) + table(1) + metric guide(1) for a normal report; for a
       comparison report (different slide shape — no ad set/chart/table/
       guide slides), just cover(1) + campaign count.
     - Added two new date-formatting helpers: formatIsoShort (abbreviated
       month) and formatSummaryRange ("Aug 14 - Aug 20, 2026"), kept
       separate from the existing formatIso/formatIsoRange (which use
       full month names and no year) so this display-only change can't
       affect date-bounds error messages or anything else that reuses
       those shared functions.

  2. Fix 2 — "Add custom title +" renamed to "Add custom PPT report
     title +" (button label and its doc comment).

  3. Fix 3 — download screen redesign (generateStatus === "done"):
     - Primary CTA ("View Report in Browser"): full width, 48px, solid
       amber (#f5b45a) background, #0d1b2e text, font-weight 600 — was
       44px with the same amber fill already but the spec called for the
       explicit 48px height.
     - Secondary row (Download PPTX / Save to Google Drive): changed from
       a CSS grid to an equal-width flex row (flex-1 each), 44px, solid
       #1e293b background with 1px amber/green borders respectively.
       Added a leading ⬇ glyph to Download PPTX and switched Save to
       Drive from the DriveIcon SVG component to a leading ☁ glyph, per
       the spec's exact button text.
     - Tertiary row (Email / WhatsApp / Copy Link): changed from a
       borderless icon-over-label column layout to three equal-width
       (flex-1) 40px buttons with solid #1e293b background and 1px
       #334155 borders, icon-beside-label. Email uses a new MailIcon
       (blue #63b3ed stroke), Copy Link uses a new CopyIcon (grey
       #94a3b8 stroke), WhatsApp keeps its existing inline SVG but now
       filled #25D366 instead of inheriting the surrounding text color.
     - Bottom "Generate Another Report" button: unchanged navy/blue-
       border style, height set explicitly to 44px with an 8px top
       margin per spec.
     - Removed the now-unused DriveIcon component (its only call site was
       replaced by the ☁ glyph); added MailIcon/CopyIcon in its place —
       both confirmed used only in this tertiary row before removing/
       adding.
     - Verified only one place in the file displays the raw share URL as
       text: the 11px muted-grey line under "View Report in Browser".
       Every other reference to buildShareReportUrl() is either an href
       (not visible text) or the clipboard-copy call — no other section
       shows a raw URL.

No other files changed. Engine files (src/lib/nre/**, src/app/api/**)
were not touched.

Verification: tsc --noEmit clean, eslint clean, npx vitest run — 1374/1374
tests passing, next build succeeds.
