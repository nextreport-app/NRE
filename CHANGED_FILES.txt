CHANGED_FILES.txt — commit af68287 "Add targeted debug logging to aggregateCpc to check for a real missing column"

src/lib/nre/dynamic-metrics.ts
  aggregateCpc(): added 2 console.log lines as the first statements in
  the function body, exactly as specified:
    console.log("aggregateCpc: headerMap keys:", [...headerMap.keys()].filter((k) => k.includes("cpc") || k.includes("click")));
    console.log("aggregateCpc: totalSpend:", totalSpend);

  This fires once per campaign/ad-set slide that selects cpc_link_click
  or cpc_all (both route through this function). The filtered headerMap
  keys list shows exactly which CPC/click-related column headers were
  actually found in this campaign's own CSV rows, before any of the
  CPC_VALUE_HEADER_CANDIDATES / CPC_CLICKS_HEADER_CANDIDATES fallback
  logic runs.

WHAT THE NEXT LOG OUTPUT WILL TELL US: if the filtered headerMap keys
array is empty for the Specialty Painting campaign's card, the CSV
genuinely has no CPC or clicks column at all — the dash is correct,
expected behavior, not a bug, and the real fix (per the user's own
framing) would be on the wizard side: don't pre-select/offer
cpc_link_click as a metric when the CSV doesn't have a column for it or
its fallback. If the array DOES contain a real header (e.g. "cpc (cost
per link click)") but the card still dashes, that points to the same
class of key-mismatch bug as the last two fixes (redistributeCardSlots'
costKey mismatch, cost_per_lead's baseline miss) — worth tracing further
before deciding on a fix.

No other files changed.

Verification: tsc --noEmit clean, eslint clean, npx vitest run —
1374/1374 tests passing, next build succeeds.
