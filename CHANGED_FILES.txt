CHANGED_FILES.txt — commit a8539aa "Add temporary debug logging to trace metrics-screen-to-PPT pipeline"

src/app/api/clients/[id]/reports/route.ts
  buildMetaData(): added 3 console.log statements right after
  selectedMetrics/campaignMetricOverrides are parsed from formData
  (before buildReportData() is called):
    REPORT ROUTE: selectedMetrics length: <n | "undefined">
    REPORT ROUTE: campaignMetricOverrides keys: [<campaign names>]
    REPORT ROUTE: campaignMetricOverrides values: <JSON>

src/lib/nre/report-data.ts
  computeMetaSlideMetrics():
    - Location 2: 3 console.log statements as the first statements in
      the function body, before the line-1044 guard
      (!selectedMetrics || selectedMetrics.length === 0 || !availableMetricsPool):
        REPORT DATA: selectedMetrics length: <n | "undefined">
        REPORT DATA: campaignMetricOverrides: <JSON>
        REPORT DATA: availableMetricsPool length: <n | "undefined">
    - Location 3: 1 console.log right after slide1Keys is computed
      (splitMetricsForSlides), inside the per-campaign/ad-set call:
        CAMPAIGN LOOP: campaign: <name> override: <string[] | undefined> slide1Keys length: <n>
      Note: the requested snippet referenced `slide.campaignName`, but
      there is no `slide` variable in this scope — computeMetaSlideMetrics
      takes `campaignName` directly as its own parameter (it's called
      once per campaign AND once per ad-set from the surrounding loop),
      so the log uses `campaignName` instead. `override` and
      `slide1Keys` are logged exactly as requested.

This function runs once per campaign slide and once per ad-set slide, so
all three of these logs (Location 2/3 especially) will fire multiple
times per report — once per campaign, then again for each selected ad
set under it, each with that ad set's own campaignName/override/
slide1Keys.

All log lines are marked "TEMPORARY DEBUG LOGGING — remove after tracing
the metrics-screen-to-PPT pipeline" for easy removal once the trace is
done.

No other files changed.

Verification: tsc --noEmit clean, eslint clean (no no-console rule in
this project's config), npx vitest run — 1374/1374 tests passing, next
build succeeds.
