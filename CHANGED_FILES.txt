CHANGED_FILES.txt — commit 2dbcab7 "Marketing and website content improvements (12 fixes)"

NEW FILE:
src/app/how-it-works/page.tsx
  Fix 1 + Fix 12. New page with: title "How NextReport Works" / sub "From
  CSV export to client-ready report in under 2 minutes"; Section 1 "What
  you need" (the two required CSV files — Previous Month Data + Last 30
  Days Day by Day); Section 2 "Report types available" (Weekly, Monthly,
  Bi-weekly, Comparison — with the exact "this week vs last week / this
  month vs last month / any custom date range" wording from Fix 12, and
  Custom Date Range); Section 3 "The 5 steps" (Upload CSV, Select
  campaigns, Confirm objectives, Review metric cards, Choose period and
  generate); Section 4 "What you get" (Download PPTX, View in browser,
  Save to Drive, Share via WhatsApp/Email).

MODIFIED:
src/app/help/download/page.tsx
  Fix 2. Full content rewrite. Meta Ads section split into two step lists
  — "For Last 30 Days (weekly reporting CSV)" (7 steps: Campaigns tab,
  Export Table Data, Last 30 Days, Time Breakdown = Day, the full column
  list, the lead-gen-specific columns, Export as CSV) and "For Previous
  Month (monthly comparison CSV)" (4 steps: Last Month, Time Breakdown
  None-or-Day, same columns, Export as CSV) — plus a callout box on why
  Result Type matters. Google Ads section rewritten to the 5-step
  Predefined reports → Basic → Campaign flow with the exact column list
  given. Title/sub updated to "Download Guide" / "How to export your
  campaign data from Meta Ads and Google Ads".

src/components/public-nav.tsx
  Fix 3. "How It Works" nav link changed from /help/download to the new
  /how-it-works page.

src/components/site-chrome-footer.tsx
  Fix 3. Footer "How it Works" link fixed to point at /how-it-works
  (was pointing at /help/download, the same URL that used to double as
  both labels); added a separate "Download Guide" link to /help/download
  right after it, so the footer now has one link per URL, no duplicates.

src/app/about/page.tsx
  Fix 4. Full rewrite. Removed: the duplicated "Monday morning" framing
  (both the hero subhead and the Story section's opening line repeated
  it — the hero subhead is gone entirely now), the "25+ hours saved per
  agency per month" stat, the StoryIllustration component and its
  favicon-large.png image, the 🇮🇳 flag emoji, and the "— NextReport Team"
  signature. New Story body is the exact 6-paragraph text given, closing
  with "— Built with Indian agencies and US client reporting in mind".
  The old 4-item emoji "What makes us different" grid (DIFFERENTIATORS)
  is removed entirely; the old numeric Stats section (STATS array) is
  replaced with three plain-text stat boxes: "Under 2 minutes per
  report", "Meta Ads and Google Ads supported", "Weekly, monthly and
  comparison reports". Mission section and CTA section unchanged.

src/app/contact/page.tsx
  Fix 5. Removed the "← Back to NextReport" link (PublicNav already
  renders on this page) and its now-unused Link import. Changed both
  text-accent (blue) links to text-[#f5b45a] (amber). Added pb-24 on
  mobile / sm:pb-16 on larger screens to the main container so the
  floating WhatsApp button can't cover the form's Send button. Added
  "We reply on WhatsApp within a few hours. Email replies within one
  business day." above the form.

src/app/pricing/page.tsx
  Fix 6. Replaced the BETA_HIDE_PRICING dead-end ("Pricing information
  coming soon... Contact us at hello@nextreport.in for access") with a
  proper beta message: "NextReport is in private beta." heading, the
  onboarding/7-day-trial paragraph, a "Get Early Access →" button linking
  to /signup, and an email + WhatsApp (wa.me/918882578327, the same
  number whatsapp-button.tsx already uses site-wide) contact line in
  amber. Header/footer (BetaBanner/PublicNav/SiteChromeFooter) unchanged
  — same as every other page. The live (non-beta) pricing branch below it
  was not touched.

src/components/home/hero-section.tsx
  Fix 7. Mock metric card numbers: "Website Leads" → "Instant Form
  Leads", count 23 → 34, "Cost per Lead" $123.78 → $8.42.

src/app/login/page.tsx, src/app/signup/page.tsx
  Fix 8. Plain-text "NextReport" h1 replaced with the logo image (36px) +
  "NextReport" Inter Bold wordmark, matching public-nav.tsx's pattern,
  wrapped in a Link back to "/".

src/components/beta-banner.tsx
  Fix 9. Mobile (<640px): px-4 py-1 and text-xs instead of the full
  py-2/text-[13px] size (sm: breakpoint restores the original size), so
  the banner takes less vertical space above the fold on phones. Dismiss
  button (×) similarly sized down on mobile.

src/components/home/features-section.tsx
  Fix 10. "Works with any CSV export" / "Any column layout from a Meta
  Ads or Google Ads export — no template to configure." changed to
  "Works with standard CSV exports" / "Works with standard Meta Ads
  Manager and Google Ads CSV exports when recommended columns are
  included." — the only other "any CSV"/"any column" occurrence found
  sitewide was in src/app/light-preview/page.tsx, which per Fix 11 is
  confirmed not linked from any public nav/footer, so it was left as-is.

VERIFIED, NO CHANGE NEEDED:
Fix 11 — grepped for `href="/light-preview"` across src/: no matches.
The page itself still exists at src/app/light-preview/page.tsx (per the
instruction to leave the page but not link it) but was not linked from
any nav, footer, or sitemap before this round either — no sitemap.ts/xml
or robots.ts exists in the repo to check.
Fix 12 — folded into the new /how-it-works page's Report Types section
(see above); no separate change needed.

No other files changed. Engine files (src/lib/nre/**), the report wizard
(src/components/report-upload-wizard.tsx), and API routes were not
touched.

Verification: tsc --noEmit clean, eslint clean (only pre-existing
<img>-vs-next/image warnings on files already using plain <img> tags
elsewhere in the codebase), npx vitest run — 1374/1374 tests passing,
next build succeeds (new /how-it-works route registered).
