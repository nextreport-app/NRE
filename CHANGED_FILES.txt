CHANGED_FILES.txt — commit 88c6438 "Replace hand-reconstructed wizard Step 2/4 UI with verified 434964d code"

src/components/report-upload-wizard.tsx
  Source of truth for this change: `git show 434964d:src/components/report-upload-wizard.tsx`
  (extracted to /tmp/wizard-434964d.tsx and diffed directly against the
  current file — not reconstructed from memory or description).

  Step 2 (campaign selection):
    - Fixed one discrepancy left over from the prior manual merge: the
      campaign name label's className was font-bold/text-white; 434964d's
      real markup uses plain text-dash-ink (no bold). Everything else in
      Step 2 already matched 434964d exactly.

  Step 4 (metric review):
    - objectiveColorClasses() (hand-rolled) replaced with 434964d's real
      objectiveAccent() function — same amber/coral/green/blue categories,
      PLUS a purple video/thruplay accent that the hand-rolled version was
      missing.
    - Card markup replaced with 434964d's exact layout:
        - borderTop applied via inline style (`3px solid <hex>`), not a
          Tailwind border-t-4 class.
        - Added "Metrics for this campaign slide" subtitle under the
          name/badge row.
        - Pills enlarged (8px/12px padding), label uppercase with letter-
          spacing, remove button color #64748b -> hover #fc8181.
        - Added a divider (border-t border-[#334155]) before the "Add
          from your CSV:" section.
        - Each addable-metric button now shows a green "+" (#68d391)
          prefix before the label — this was the "green + signs" the
          request specifically asked for and the hand-rolled version did
          not have.

  Engine wiring preserved, NOT reverted to 434964d's older version:
    - MAX_METRICS_PER_SLIDE (8-card cap) — grafted onto the restored
      "Add from your CSV" button's disabled attribute, since 434964d
      predates this cap (added later by the three-layer rebuild).
    - metricsLimitMessage — still displayed per-campaign card.
    - perCampaignMetrics / perCampaignAvailablePool state, addCampaignMetric
      / removeCampaignMetric / campaignAvailableMetrics functions — all
      unchanged, only their JSX callers were touched.

No other files changed. Engine files (src/lib/nre/**, src/app/api/**) were
not touched.

Verification: tsc --noEmit clean, eslint clean, npx vitest run — 1374/1374
tests passing, next build succeeds.
