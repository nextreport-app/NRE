CHANGED_FILES.txt — commit 996f11c "Add debug logging directly in the generate route's POST handler"

src/app/api/clients/[id]/reports/route.ts
  Two more TEMPORARY DEBUG LOGGING lines, both directly inside POST()
  itself, not inside buildMetaData (a helper POST calls later):

  1. First statement in the function, before auth/client-lookup/
     subscription-guard/validation can return early:
       console.log("REPORT ROUTE POST: handler entered, url:", req.url);
     If a generate request produces zero output from this route — not
     even this one line — the request never reached this handler at all
     (routing/network/deployment issue), not a data issue further down.

  2. Right after formData is parsed, before any schema parsing/filtering
     (parseJsonFormField/campaignMetricOverridesSchema etc. haven't run
     yet at this point):
       console.log("REPORT ROUTE POST: raw campaignMetricOverrides field:", formData ? formData.get("campaignMetricOverrides") : "no formData");
     This is the rawest possible read — straight off the multipart form
     field the client actually posted, before any validation/parsing
     could silently drop or reshape it.

Combined with the existing "REPORT ROUTE: ..." logs already added inside
buildMetaData() (previous commit a8539aa), a generate request now logs at
three points along the way: handler entry, raw form field, and parsed
field — enough to isolate whether a missing-override symptom is a
routing issue (log 1 missing), a client-payload issue (log 2 missing/
empty), or a parsing/schema issue (log 2 present but the existing
buildMetaData log shows it empty).

No other files changed.

Verification: tsc --noEmit clean, eslint clean, npx vitest run —
1374/1374 tests passing, next build succeeds.
