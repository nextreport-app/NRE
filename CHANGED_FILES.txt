CHANGED_FILES.txt — commit 1c889ab "Replace aggregateCpc debug logging with per-candidate row-value detail"

src/lib/nre/dynamic-metrics.ts
  aggregateCpc(): replaced the previous 2-line headerMap/totalSpend log
  (added last round, already confirmed the "cpc (cost per link click)"
  header is present with totalSpend $269.08) with a per-candidate log
  inside the CPC_VALUE_HEADER_CANDIDATES loop:

    const allValues = rows.map((r) => r._raw?.[header]).slice(0, 5);
    const nonZero = rows.map((r) => parseCellNum(r._raw?.[header])).filter((v) => v > 0);
    console.log("aggregateCpc candidate:", candidate, "allValues(first5):", allValues, "nonZero count:", nonZero.length);

  Fires once per matching candidate header, per campaign/ad-set slide
  that selects cpc_link_click or cpc_all.

WHAT THE NEXT LOG OUTPUT WILL TELL US: the first 5 raw string values from
the CSV's own "cpc (cost per link click)" column for this campaign's
rows, plus how many parsed to a real nonzero number. This settles the
open question from this round's investigation: is the dash genuinely
correct (all real values are blank/zero/"0.00" for this campaign — in
which case redistributeCardSlots already correctly drops it as an empty
secondary slot, and fill-tags.ts's unused-template-position dash is
expected, designed behavior, not a bug), or is there a real value in the
raw CSV that isn't being parsed/matched correctly (a genuine bug,
somewhere in parseCellNum, header resolution, or elsewhere)?

No other files changed.

Verification: tsc --noEmit clean, eslint clean, npx vitest run —
1374/1374 tests passing, next build succeeds.
