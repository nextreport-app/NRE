/**
 * Single on/off switch for every pricing-related UI hidden during the
 * private beta — see each usage site's own "// BETA: hidden during beta
 * period" comment for what it gates. Flip this back to `false` to restore
 * pricing everywhere at once when the beta ends; nothing else needs to
 * change.
 */
export const BETA_HIDE_PRICING = false;
